[workspace]
members = [
    "programs/donation_pool",
    "programs/nft_tracker",
]

[provider]
cluster = "Devnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 tests/*.ts"
